# SDKMAN environment setup
# Source this file or use direnv to automatically load it
# 
# Usage: source .envrc
# Or install direnv: https://direnv.net/

# Load SDKMAN if available
if [ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]; then
    source "$HOME/.sdkman/bin/sdkman-init.sh"
    sdk use java 11.0.28-tem
    sdk use gradle 8.14
    sdk use kotlin 1.8.0
fi

